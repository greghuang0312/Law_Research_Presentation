# Curation Module

Task 5 starts the deterministic curation layer that turns retrieval results into:

- `CaseMaterial[]`
- `LawReference[]`
- `SummaryBlock[]` with embedded `slide_plan[]`

Current scope in this batch:

- case-oriented extraction from stored `SourceItem[]`
- law/policy-oriented extraction from stored `SourceItem[]`
- chapter and slide outline builders

API orchestration is added in the next Task 5 batch.
